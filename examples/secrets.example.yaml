# Copy this file to secrets.yaml and replace the placeholders with your values.
# The MyHOME gateway network settings are usually configured through the UI config entry,
# not in myhome.yaml, but helper packages and external bridges may still need secrets.

myhome_primary_host: "192.168.1.35"
myhome_primary_password: "YOUR_OPENWEBNET_PASSWORD"
myhome_aux_host: "192.168.1.36"
myhome_aux_password: "YOUR_OPENWEBNET_PASSWORD"
google_assistant_project_id: "your-google-project-id"
